# 04.2 回测层工程实现与规范
> 本文档描述 Alphalens、Backtrader 和候选信号脚本的实际接口、路径、参数和输出。
---
## 一、文件职责
| 文件 | 职责 | V2状态 |
|:---|:---|:---|
| `alphalens_analysis.py` | IC、分层收益、稳定性和HTML报告 | 已兼容 |
| `backtrader.eval.py` | 月度调仓和资金回测 | 部分兼容 |
| `generate_live_signals.py` | 候选股票和目标金额 | 尚未兼容 |
| `utils.py` | 预测、价格和报告目录工具 | 已兼容Alphalens |
| `backtrader_eval_有金额上限.py` | 独立实验版本 | 非默认入口 |
---
## 二、输入规范
### 2.1 预测
```text
03模型训练层/experiments/{exp_id}/predictions.parquet
03模型训练层/experiments/{exp_id}/smoothed_predictions.parquet
```

字段：
| 字段 | 说明 |
|:---|:---|
| date | 预测日期 |
| stock_code | 股票代码 |
| pred_score | 原始预测 |
| pred_score_smooth | 平滑预测 |

### 2.2 行情
```text
02因子库/processed_data/market_data/
├── open.parquet
├── high.parquet
├── low.parquet
├── close.parquet
└── volume.parquet
```

### 2.3 ST状态
正式目标：

```text
01数据/data/tushare_data/st_status.parquet
```

当前两个交易脚本仍使用旧：

```text
01数据/data/raw_data/st_status.parquet
```
---
## 三、Alphalens
### 3.1 运行
```powershell
python 04回测层/alphalens_analysis.py `
  --exp-id lgbm20_profit20_full_v2 `
  --periods 20 `
  --quantiles 10 `
  --use-smooth
```

### 3.2 参数
| 参数 | 默认 | 说明 |
|:---|:---|:---|
| `--exp-id` | 必填 | 实验目录 |
| `--periods` | 20 | 远期收益周期 |
| `--quantiles` | 10 | 分位数组数 |
| `--use-smooth` | false | 使用平滑预测 |

### 3.3 数据处理
```text
预测长表
  │
  ▼
MultiIndex(date, asset)
  │
  + 收盘价宽表
  ▼
get_clean_factor_and_forward_returns
```

### 3.4 输出
```text
reports/{exp_id}/
├── alphalens_report[_smooth].html
├── ic_analysis[_smooth].png
├── returns_analysis[_smooth].png
└── turnover_analysis[_smooth].png
```
---
## 四、Backtrader
### 4.1 参数配置
当前参数直接定义在代码：

```python
STRATEGY_PARAMS = {
    "stocks_per_batch": 20,
    "start_date": datetime(2023, 10, 1),
    "end_date": datetime(2026, 6, 30),
    "initial_cash": 50000,
    "commission": 0.002,
}
```

策略内部：

```python
stop_loss = 0.2
target_total_position = 0.90
```

### 4.2 运行
```powershell
python 04回测层/backtrader.eval.py `
  --exp-id lgbm20_profit20_full_v2 `
  --use-smooth
```

### 4.3 参数
| 参数 | 默认 | 说明 |
|:---|:---|:---|
| `--exp-id` | `exp_001` | 实验目录 |
| `--use-smooth` | false | 读取平滑预测 |

回测日期、资金、持仓数和手续费目前不能通过 CLI 覆盖。

### 4.4 数据加载
1. 读取主预测文件
2. 尝试读取旧 live 文件
3. live 文件不存在时使用空表继续
4. 合并 OHLCV 宽表
5. 删除行情缺失行

V2 只有单一 PIT 主预测链，因此第 2 步属于兼容遗留，后续应删除。

### 4.5 信号生成
```text
按月取首个交易日
  → 主板过滤
  → ST过滤
  → 有效数据>=20天
  → T+1开盘涨停过滤
  → prediction降序Top20
  → 生成买卖差集
```

### 4.6 策略执行
- 调仓日先处理卖出列表
- 买入列表按 90% 总仓位等权
- 每日检查止损
- Backtrader 记录订单成交

当前无显式滑点设置。

### 4.7 输出
```text
reports/{exp_id}/
├── rebalance_signals.csv
├── trades.csv
├── equity_curve.png
└── backtest_report.html
```

当前不生成 `performance.json`。
---
## 五、候选实盘信号
### 5.1 当前命令
```powershell
python 04回测层/generate_live_signals.py `
  --exp-id <exp_id> `
  --top-n 20 `
  --total-cash 100000 `
  --date 2026-07-28
```

### 5.2 CLI参数
| 参数 | 默认 | 说明 |
|:---|:---|:---|
| `--exp-id` | 必填 | 实验目录 |
| `--top-n` | 20 | 股票数量；help文案仍误写30 |
| `--total-cash` | 100000 | 总资金 |
| `--date` | 最新预测日 | 指定信号日期 |
| `--output-dir` | reports/{exp_id} | 输出目录 |

### 5.3 当前不兼容点
代码强制读取：

```text
predictions.parquet
live_predictions.parquet
```

V2 不生成 live 文件，因此脚本在读取阶段失败。

此外：
- 不支持 `--use-smooth`
- ST路径仍是旧数据
- 最新日没有T+1行情时无法执行开盘涨停过滤

### 5.4 计划输出
```text
reports/{exp_id}/live_signals_YYYYMMDD.csv
```

该输出在完成V2迁移前不能作为正式下单输入。
---
## 六、工程规范
### 6.1 路径
- 使用 `Path(__file__).parent.parent` 获取项目根
- 正式 ST 路径必须指向 `tushare_data`
- 不使用个人绝对路径

### 6.2 实验ID
CLI 接收的是实际目录名。V2 单模型通常以 `_v2` 结尾，融合实验是否带后缀取决于 `--output-exp`。

### 6.3 预测列
| 文件 | 列 |
|:---|:---|
| predictions | `pred_score` |
| smoothed_predictions | `pred_score_smooth` |

### 6.4 报告真实性
报告只列实际启用参数：
- 已启用手续费
- 未启用显式滑点
- 已启用20%止损
- 未启用行业中性和换手约束

### 6.5 最新日信号
T+1开盘涨停属于未来成交状态。在 T 日收盘生成候选信号时只能标注待开盘复核，不能假装已经知道 T+1 开盘价。
---
## 七、常见问题
| 问题 | 原因 | 处理 |
|:---|:---|:---|
| Backtrader提示缺live文件 | V2不生成live文件 | 当前可忽略，主预测继续运行 |
| 实盘信号直接失败 | 强制读取live文件 | 需先迁移代码 |
| ST过滤日期落后 | 使用旧QMT状态表 | 切换Tushare路径 |
| 报告写15%但结果按20% | 日志文案旧 | 以参数0.2为准并修正文案 |
| 没有performance.json | 当前代码未输出 | 不把它列为现有产物 |
| 最新日无法涨停过滤 | 没有T+1开盘价 | 开盘时二次过滤 |
---
*文档版本: v3.0*
*最后更新: 2026-07-30*
*维护者: 蒋大王*
